import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DreamCollege.ai Passion Project Builder - Discover Your Perfect Extracurricular Activities",
  description: "Get personalized recommendations for passion projects and extracurricular activities that align with your interests, academic strengths, and future goals.",
  keywords: "extracurricular activities, passion projects, college applications, student activities, personalized recommendations, DreamCollege.ai",
  authors: [{ name: "DreamCollege.ai" }],
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
